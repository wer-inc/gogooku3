apiVersion: 1

datasources:
  # Prometheus datasource
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: true

  # ClickHouse datasource
  - name: ClickHouse
    type: clickhouse-datasource
    access: proxy
    jsonData:
      server: clickhouse
      port: 8123
      username: default
      defaultDatabase: gogooku3
    secureJsonData:
      password: gogooku123
    editable: true

  # Redis datasource
  - name: Redis
    type: redis-datasource
    access: proxy
    jsonData:
      client: standalone
      host: redis:6379
    secureJsonData:
      password: gogooku123
    editable: true
