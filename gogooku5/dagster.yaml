# Dagster instance configuration for gogooku5 dataset builds.
# Uses default ephemeral storage.

telemetry:
  enabled: false

run_launcher:
  module: dagster.core.launcher
  class: DefaultRunLauncher

run_storage:
  module: dagster.core.storage.runs
  class: SqliteRunStorage
  config:
    # Use a path under .dagster/ so Dagster artifacts are clearly separated.
    # Effective path: <DAGSTER_HOME>/.dagster/storage
    base_dir: .dagster/storage

event_log_storage:
  module: dagster.core.storage.event_log
  class: SqliteEventLogStorage
  config:
    # Effective path: <DAGSTER_HOME>/.dagster/storage
    base_dir: .dagster/storage
